name: Keep Alive

on:
  schedule:
    - cron: '0 */6 * * *'  # every 6 hours
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Relay
        run: |
          status=$(curl -s -o /dev/null -w "%{http_code}" https://cx-relay.streamlit.app/)
          echo "Response: $status"
